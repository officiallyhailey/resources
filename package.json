{
  "scripts": {
    "dev": "npm run dev --prefix resources",
    "build": "npm run build --prefix resources",
    "deploy": "npm run deploy --prefix resources",
    "watch:scss": "node-sass -w editing/style.scss editing/style.css"
  },
  "dependencies": {
    "node": "^21.2.0",
    "node-sass": "^9.0.0"
  }
}
